name: astroyorumai_desktop_test
description: "Desktop-only test version without Firebase"
publish_to: 'none'
version: 2.1.3+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.1.0
  intl: ^0.18.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  table_calendar: ^3.0.9
  # Crypto for horary calculations
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true
  
  assets:
    - assets/fonts/
